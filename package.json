{
  "name": "evolution-api",
  "version": "2.1.2",
  "description": "Evolution API WhatsApp",
  "main": "dist/main.js",
  "scripts": {
    "dev": "tsnd -r tsconfig-paths/register --files --transpile-only ./src/main.ts",
    "start": "npm run start:prod",
    "start:prod": "node dist/main.js",
    "build": "tsup ./src/main.ts --format esm,cjs --target esnext --dts --outDir dist --clean"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@prisma/client": "^5.12.0",
    "axios": "^1.6.8",
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.18.3"
    // adicione aqui os outros pacotes que o projeto realmente usa
  },
  "devDependencies": {
    "tsup": "^7.2.0",
    "typescript": "^5.3.3",
    "tsconfig-paths": "^4.2.0"
  }
}
